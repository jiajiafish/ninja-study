var counter = function(arr)
{
    return 'therr are'+arr.length+"elements"
}

var adder = function(a,b)
{
    return `sum is ${a+b}`
}
var pi = 3.142

module.exports = {
    counter:counter,
    adder:adder,
    pi:pi
}